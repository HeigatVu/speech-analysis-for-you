---
slug: vietnamese-transcription-pipeline
date: 2026-09-21
version: 4
---

# Tasks: reliable Vietnamese transcription and audio profiles

Statuses in [tasks-2026-09-21.json](./tasks-2026-09-21.json) must change together. Take the first queued task whose dependencies are complete. Keep private corpus artifacts outside git.

## T1: Annotation contract and package scaffold [done]

**Goal:** Add immutable approved-annotation models and stable redacted validation errors.

**Acceptance:** Valid synthetic v1 parses deeply immutable; unknown keys, absolute paths, malformed hashes, duplicate IDs, overlaps, invalid roles, stale hashes, and draft input fail with stable codes. No transcript or raw exception is logged.

**Verify:** `python -m pytest tests/say_transcribe/test_annotations.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** none  
**Files:** `pyproject.toml`, `src/say_transcribe/__init__.py`, `src/say_transcribe/annotations.py`, `tests/say_transcribe/test_annotations.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-1-annotations`  
**Scope:** M

## T2: Channel-safe native task clips [queued]

**Goal:** Verify a master hash, select the declared channel, crop native PCM sample bounds, and expose a separate in-memory 16 kHz ASR view.

**Acceptance:** Active-left/silent-right fixture proves no mean downmix; source rate/width and sample boundaries are preserved. Invalid channels/spans/PCM and hash mismatches fail before output. Provenance is redacted.

**Verify:** `python -m pytest tests/say_transcribe/test_audio_prepare.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T1  
**Files:** `src/say_transcribe/audio.py`, `src/say_transcribe/prepare.py`, `tests/say_transcribe/test_audio_prepare.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-2-channel-clips`  
**Scope:** M

## T3: SoX/GSM common-channel profile [queued]

**Goal:** Add the `tell-inspired-v1` artifact contract and deterministic SoX stage.

**Acceptance:** Explicit mono input goes through SoX GSM Full Rate 8 kHz/13 kbps round-trip, PCM16 decode, `rate -v` to 16 kHz, 200 Hz high-pass, and 3.4 kHz low-pass. No `compand` is added. Missing SoX/GSM and length/clipping drift produce stable errors. Native clips remain unchanged.

**Verify:** synthetic subprocess-wrapper tests; `python -m pytest tests/say_transcribe/test_preprocess_contract.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T2  
**Files:** `src/say_transcribe/audio_profile.py`, `src/say_transcribe/preprocess.py`, `tests/say_transcribe/test_preprocess_contract.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-3-sox-profile`  
**Scope:** M

## T4: Loudness and DeepFilterNet3 stage [queued]

**Goal:** Complete the optional profile and write privacy-safe provenance.

**Acceptance:** FFmpeg two-pass `loudnorm` uses `I=-23`, `LRA=7`, `TP=-2`, `linear=true`, `dual_mono=false`; DeepFilterNet3 v0.5.6 is lazy, pinned, hash-checked, delay-compensated, and never auto-downloaded by tests. Sidecar records public stage configs/measures, hashes, revisions, and issue codes only. Unmeasurable loudness, missing tools, altered model, clipping, and alignment drift fail closed.

**Verify:** `python -m pytest tests/say_transcribe/test_preprocess_pipeline.py tests/say_transcribe/test_provenance.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T3  
**Files:** `src/say_transcribe/preprocess.py`, `src/say_transcribe/provenance.py`, `tests/say_transcribe/test_preprocess_pipeline.py`, `tests/say_transcribe/test_provenance.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-4-loudness-denoise`  
**Scope:** M

## T5: Silero VAD artifact [queued]

**Goal:** Add the pinned CPU ONNX VAD adapter and interval sidecar validation.

**Acceptance:** Silero v6.2.2 model revision/hash and defaults (0.5 threshold, 250 ms speech, 100 ms silence, 30 ms padding, 16 kHz) are explicit. Intervals are bounded half-open integer samples; model loading is lazy/per-process; missing or altered model fails with stable codes.

**Verify:** `python -m pytest tests/say_transcribe/test_vad.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T4  
**Files:** `src/say_transcribe/vad.py`, `src/say_transcribe/preprocess.py`, `tests/say_transcribe/test_vad.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-5-silero-vad`  
**Scope:** M

## T6: `silero_timing` feature pack [queued]

**Goal:** Consume validated VAD intervals without changing existing native packs.

**Acceptance:** Optional manifest `preprocess_path` is hash-checked. Approved participant intersections emit all `svad_*` keys in the spec; 0 pause count/total is distinguished from no speech; no-speech/missing-target cases produce `NaN` plus issues. Processed pitch/HNR/energy/spectral values are not published.

**Verify:** `python -m pytest tests/speech_features/test_silero_timing.py tests/speech_features/test_manifest.py -q`; `python -m ruff check src/speech_features tests/speech_features`.

**Depends:** T5  
**Files:** `src/speech_features/schema.py`, `src/speech_features/extraction.py`, `src/speech_features/features/acoustic/silero_timing.py`, `tests/speech_features/test_silero_timing.py`, `tests/speech_features/test_manifest.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-6-silero-timing`  
**Scope:** M

## T7: PhoWhisper-medium to `SpeechDocument` [queued]

**Goal:** Add one lazy real adapter and one fake backend.

**Acceptance:** Fake ASR produces valid timed Vietnamese JSON v2; real configuration selects `vinai/PhoWhisper-medium`, preserves model segments, and fails safely when CUDA/model dependencies are unavailable. No downloads or heavy imports occur in unit tests.

**Verify:** `python -m pytest tests/say_transcribe/test_asr.py -q`; `python -m speech_features.cli validate tests/say_transcribe/fixtures/fake-output.json`.

**Depends:** T2  
**Files:** `src/say_transcribe/asr.py`, `src/say_transcribe/document_builder.py`, `tests/say_transcribe/test_asr.py`, `tests/say_transcribe/fixtures/fake-output.json`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-7-phowhisper-document`  
**Scope:** M

## T8: Vietnamese word grouping [queued]

**Goal:** Add Underthesea 9.5.0 grouping without rewriting surfaces.

**Acceptance:** Multi-syllable words share `word_id`; NFC, tone marks, punctuation, and `d`/`đ` remain unchanged. Alignment failures leave only affected tokens ungrouped and emit `WORD_GROUPING_UNALIGNED`; no cross-utterance grouping.

**Verify:** `python -m pytest tests/say_transcribe/test_word_grouping.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T7  
**Files:** `src/say_transcribe/word_grouping.py`, `tests/say_transcribe/test_word_grouping.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-8-word-grouping`  
**Scope:** S

## T9: Participant-safe JSON and CHAT [queued]

**Goal:** Serialize existing JSON v2 and optional CHAT from approved turns.

**Acceptance:** Midpoint assignment excludes examiner/ambiguous/unapproved time, role changes split utterances, JSON reloads, and requested CHAT passes Chatter. Errors contain codes/positions only.

**Verify:** `python -m pytest tests/say_transcribe/test_formats.py tests/say_transcribe/test_speakers.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T5, T8  
**Files:** `src/say_transcribe/speakers.py`, `src/say_transcribe/formats.py`, `tests/say_transcribe/test_speakers.py`, `tests/say_transcribe/test_formats.py`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-9-json-chat`  
**Scope:** M

## T10: CLI, provenance, and manifest routing [queued]

**Goal:** Expose preprocessing and explicit native/processed routing.

**Acceptance:** `preprocess` and `run --asr-audio native|tell-inspired-v1` are opt-in/atomic; native is default. Manifest v2 accepts optional relative `preprocess_path`; provenance and logs contain no forbidden data. `--force` cannot replace source annotations or escape the output directory.

**Verify:** `python -m pytest tests/say_transcribe/test_cli.py tests/say_transcribe/test_provenance.py -q`; `python -m speech_features.cli validate tests/say_transcribe/fixtures/manifest-v2.json`; `python -m ruff check src/say_transcribe tests/say_transcribe`.

**Depends:** T6, T9  
**Files:** `src/say_transcribe/cli.py`, `src/say_transcribe/provenance.py`, `tests/say_transcribe/test_cli.py`, `tests/say_transcribe/test_provenance.py`, `pyproject.toml`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-10-cli-routing`  
**Scope:** M

## T11: Private A/B evaluator and promotion gate [queued]

**Goal:** Compare native and processed ASR/timing outputs before promotion.

**Acceptance:** Private development/held-out manifests remain untracked. Synthetic tests prove NFC/casefold normalization, SyER, CER, frozen-Underthesea WER, diagnostic DER, timing agreement, format rates, counts, and seeded 2,000-resample clip-bootstrap intervals. Report aggregate values only and state the gate decision.

**Verify:** `python -m pytest tests/say_transcribe/test_evaluate.py -q`; `python -m ruff check src/say_transcribe tests/say_transcribe`; `git diff --check`.

**Depends:** T10  
**Files:** `src/say_transcribe/evaluate.py`, `tests/say_transcribe/test_evaluate.py`, `docs/2026-09-21/vietnamese-transcription-pipeline/4/BENCHMARK-2026-09-21.md`  
**Branch:** `feature/vietnamese-transcription-pipeline-task-11-evaluator`  
**Scope:** M

## T12: Private GPU pilot handoff [blocked]

**Goal:** Run the real PhoWhisper-medium pilot only after the host is repaired.

**Acceptance:** `nvidia-smi` and a PyTorch CUDA smoke test succeed; a private approved run records redacted aggregate quality, runtime, peak VRAM, artifact compatibility, and reviewer sign-off. No corpus artifact enters git.

**Verify:** `nvidia-smi`; `python -c "import torch; assert torch.cuda.is_available()"`; `python -m pytest -q`; `python -m ruff check src tests`; `git diff --check`.

**Depends:** T11  
**Files:** `docs/2026-09-21/vietnamese-transcription-pipeline/4/BENCHMARK-2026-09-21.md`, `docs/README.md`  
**Branch:** `chore/vietnamese-transcription-pipeline-task-12-pilot-handoff`  
**Scope:** S + private runtime artifacts

**Blocker:** `nvidia-smi` cannot currently communicate with the NVIDIA driver. Change to queued only after both smoke checks succeed.

## Checkpoints

- **CP1 (T1/T2/T7):** trusted annotation, native channel-safe clip, and fake ASR-to-JSON slice.
- **CP2 (T3–T6):** validated processed sidecar, Silero intervals, and separate timing pack with native regression tests.
- **CP3 (T8–T10):** participant-safe Vietnamese JSON/CHAT and label-free dual-track manifest.
- **CP4 (T11/T12):** measured gate and, only if unblocked, private GPU pilot.
